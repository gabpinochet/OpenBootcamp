let altura_cm = 172
let altura_m = 1.72
let peso_kg = 85.60;
let altura_red = altura_m.toFixed(0);
let peso_red = Math.floor(peso_kg);
let is_max = ((Number.MAX_VALUE + 1) == Number.MAX_VALUE)

console.log(altura_red)
console.log(peso_red)
console.log(is_max)